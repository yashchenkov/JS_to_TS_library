import { Configuration, EndpointInfo, IMiddleware, Req, Res } from "@tsed/common";
/**
 * @middleware
 */
export declare class MultipartFileMiddleware implements IMiddleware {
    private configuration;
    private multer;
    constructor(configuration: Configuration);
    use(endpoint: EndpointInfo, request: Req, response: Res): Promise<any>;
    invoke(conf: any): any;
}
